/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow the frontend to talk to the backend on a different origin
  async headers() {
    return [
      {
        source: '/(.*)',
        headers: [
          { key: 'X-Content-Type-Options', value: 'nosniff' },
          { key: 'X-Frame-Options', value: 'DENY' },
        ],
      },
    ]
  },
}

export default nextConfig
